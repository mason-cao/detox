# Sparkle Tools

Sparkle command-line tools are intentionally not committed.

Install locally from the pinned Sparkle archive:

```bash
tar -xJf /tmp/Sparkle-2.9.1.tar.xz -C infra/build ./bin/sign_update ./bin/generate_keys
```

`sparkle_sign.sh` uses `bin/sign_update` when present.
